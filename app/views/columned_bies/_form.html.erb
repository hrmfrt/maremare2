<%= form_with(model: columned_by) do |form| %>
  <% if columned_by.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(columned_by.errors.count, "error") %> prohibited this columned_by from being saved:</h2>

      <ul>
        <% columned_by.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group form-horizontal">
    <%= form.label :"名前 (ニックネームでいれること)" , {class: "col-sm-3 control-label"}  %>
    <%= form.text_field :name, {class: "col-sm-6 form-control"}   %>
  </div>

 <div class="actions">
    <%= form.submit :"Update", class: "btn btn-outline-secondary" %>
  </div>
<% end %>
