<%= render './maregiment/maregiment_header'%>

<!--もどるぼたん-->
<div>投稿した直後は、すすむボタンがでなくなるまで、押してください。</div>
<% if Photo.exists?(@photo.id-1)%>
  <%= link_to "もどる", "/photos/#{@photo.id-1}" %>
<% else %>
<% end%>
<!--すすむボタン-->
<% if Photo.exists?(@photo.id+1)%>
  <%= link_to "すすむ", "/photos/#{@photo.id+1}" %>
<% else %>
<% end%>


  <p id="notice"><%= notice %></p>
<div class="form_wrapper" style="padding:20px">

<p id="notice"><%= notice %></p>

<p>
  <strong>Photo:</strong>
  <%= @photo.id %>
</p>

<p>
  <strong>Caption:</strong>
  <%= @photo.caption %>
</p>

<p>
  <strong>Picture:</strong>
  <%= image_tag PhotoSet.find(@photo.photo_set_id).image_sets.find(@photo.photo_num).variant( resize: "300" ) %>
  <%= image_tag PhotoSet.find(@photo.photo_set_id).image_sets.find(@photo.photo_num).variant( resize: "500" ) %>
</p>


<%= link_to 'Edit', edit_photo_path(@photo) %> |
<%= link_to 'Back', "/photosetlist/#{PhotoSet.find(@photo.photo_set_id).name}" %>
